[pytest]
testpaths = tests
pythonpath = src tests
markers =
    checklist(row, tier): the D4.3 conformance checklist row this test evidences
filterwarnings =
    error::DeprecationWarning:sort4circ_dpp.*
